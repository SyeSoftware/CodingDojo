This kata requires some node modules, so, please run the
following commands inside javascript/potter folder.

`$ npm install mocha`
`$ npm install sugar`

In order to test potter kata run:

`$ mocha potter_test.js`
